

<%= form_for(@course) do |f| %>
  <% if @course.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@course.errors.count, "error") %> prohibited this course from being saved:</h2>

      <ul>
      <% @course.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :code %><br />
    <%= f.text_field :code, :placeholder => "Please, Enter Code" %>
  </div>
  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name, :placeholder => "Please, Enter Name" %>
  </div>
  <div class="field">
    <%= f.label :teacher_id %><br />
	  <%= f.collection_select :teacher_id, @teachers, :id, :full_name, :include_blank => "Select a Teacher" %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

